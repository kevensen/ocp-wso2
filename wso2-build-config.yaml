kind: "BuildConfig"
apiVersion: "v1"
metadata:
  name: "wso2-pipeline"
spec:
  source:
    type: "Git"
    git:
      uri: "https://github.com/kevensen/ocp-wso2.git"
  strategy:
    type: "JenkinsPipeline"
    jenkinsPipelineStrategy:
